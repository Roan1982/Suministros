# Archivo de ejemplo para variables de entorno en desarrollo
# Copia este archivo a .env y configura tus valores locales

# Clave secreta de Django (genera una nueva para producción)
DJANGO_SECRET_KEY=django-insecure-nueva-clave-segura-2025

# Hosts permitidos (separados por coma)
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Configuración de base de datos
DATABASE_NAME=tu_base_de_datos
DATABASE_USER=tu_usuario
DATABASE_PASSWORD=tu_contraseña
DATABASE_HOST=localhost
DATABASE_PORT=3306

# Para Docker
POSTGRES_DB=stockdb
POSTGRES_USER=stockuser
POSTGRES_PASSWORD=tu_contraseña_segura

# Path del proyecto (para wsgi_production.py)
PROJECT_PATH=/ruta/a/tu/proyecto/stock